name: remote ssh command
on: [push]
jobs:

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - name: executing remote ssh commands using password
      uses: appleboy/ssh-action@v1.0.0
      with:
        host: sertis.by
        username: bitrix130
        key: ${{ secrets.SERTIS_BY_SSH }}
        port: 22
        script: |
          cd /home/bitrix130/domains/sertis.by/public_html
          git pull
